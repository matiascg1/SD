# Configuración generada automáticamente desde experiments.yaml
# Experimento: Experimento 1: LRU + Poisson (Baseline)

# Dataset
DATA_PATH=data/test.csv

# Cache Configuration
CACHE_HOST=localhost
CACHE_PORT=6379
CACHE_DB=0
CACHE_TTL_SECONDS=3600
CACHE_POLICY=LRU
CACHE_MAX_SIZE=10000

# LLM Configuration
LLM_PROVIDER=OLLAMA
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL_NAME=llama3.2

# Database Configuration
DB_TYPE=SQLITE
SQLITE_DB_PATH=data/results_exp1.db

# Traffic Configuration
TRAFFIC_DISTRIBUTION_TYPE=POISSON
TRAFFIC_LAMBDA=0.5
TRAFFIC_NUM_REQUESTS=5
TRAFFIC_MAX_DELAY_SECONDS=2